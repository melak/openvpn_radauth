diff --git a/Makefile b/Makefile
index cf6929e..25fbd3e 100644
--- a/Makefile
+++ b/Makefile
@@ -11,6 +11,9 @@ ifeq ($(WITH_SSL_IMPL),polarssl)
 LDFLAGS		+= -lpolarssl
 else
 LDFLAGS		+= -lcrypto
+ifeq ($(strip $(TARGET)),brcm-2.4-kamikaze)
+LDFLAGS		+= -ldl
+endif
 endif
 
 export		WITH_SSL_IMPL
diff --git a/Makefile.rules b/Makefile.rules
index 79ace10..3d2153d 100644
--- a/Makefile.rules
+++ b/Makefile.rules
@@ -19,6 +19,15 @@ LIBCVER		:= 0.9.30.1
 CROSS_ARCH	:= mips
 endif		# TARGET=ar71xx-backfire
 
+ifeq ($(strip $(TARGET)),brcm-2.4-kamikaze)
+ROOT		:= /opt/brcm-2.4
+STAGING_DIR	:= $(ROOT)/staging_dir
+ARCHITECTURE	:= mipsel
+COMPILERVER	:= 3.4.6
+LIBCVER		:= 0.9.30.1
+CROSS_ARCH	:= mipsel
+endif		# TARGET=brcm-2.4-kamikaze
+
 ifneq ($(strip $(ROOT)),)
 TOOLCHAIN_DIR	:= $(STAGING_DIR)/toolchain-$(ARCHITECTURE)_gcc-$(COMPILERVER)_uClibc-$(LIBCVER)
 TARGET_DIR	:= $(STAGING_DIR)/target-$(ARCHITECTURE)_uClibc-$(LIBCVER)
@@ -28,6 +37,13 @@ PATH		:= $(PATH):$(TOOLCHAIN_DIR)/bin:$(TOOLCHAIN_DIR)/usr/bin
 
 CFLAGS		+= -I$(TARGET_DIR)/usr/include
 LDFLAGS		+= -L$(TARGET_DIR)/usr/lib
+
+ifeq ($(strip $(TARGET)),brcm-2.4-kamikaze)
+CFLAGS		+= -I$(TOOLCHAIN_DIR)/usr/include
+LDFLAGS		+= -L$(TOOLCHAIN_DIR)/lib -L$(TOOLCHAIN_DIR)/usr/lib
+LDFLAGS		+= $(foreach rp,$(TOOLCHAIN_DIR)/lib $(TOOLCHAIN_DIR)/usr/lib,-Wl,-rpath-link,$(rp))
+endif		# TARGET=brcm-2.4-kamikaze
+
 CFLAGS		+= -DHAVE_STRL
 SUFFIX		:= _$(TARGET)
 endif		# ROOT!=""
@@ -69,7 +85,7 @@ CFLAGS		+= -Wformat-security -Waggregate-return
 
 CFLAGS		+= -fno-common -Wp,-D_FORTIFY_SOURCE=2
 
-ifeq ($(filter $(TARGET),brcm-2.4),)
+ifeq ($(filter $(TARGET),brcm-2.4-kamikaze),)
 CFLAGS		+= -Wmissing-field-initializers -Wmissing-include-dirs -Wunsafe-loop-optimizations
 endif
 
